const { createClient } = require('@supabase/supabase-js'); const https = require('https'); require('dotenv').config(); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function subscribePages() { const { data: pages } = await supabase.from('connected_pages').select('page_id, access_token'); if (!pages) return console.log('No pages found'); for (const page of pages) { const url = \https://graph.facebook.com/v19.0/\/subscribed_apps?subscribed_fields=feed,messages&access_token=\\; console.log(\Subscribing \...\); await new Promise(resolve => { const req = https.request(url, { method: 'POST' }, (res) => { let responseData = ''; res.on('data', chunk => responseData += chunk); res.on('end', () => { console.log(\Response for \:\, responseData); resolve(); }); }); req.on('error', console.error); req.end(); }); } } subscribePages();
